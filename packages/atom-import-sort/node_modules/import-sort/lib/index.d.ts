/// <reference types="core-js" />
import { IParser } from "import-sort-parser";
import { IStyle } from "import-sort-style";
export interface ISortResult {
    code: string;
    changes: Array<ICodeChange>;
}
export interface ICodeChange {
    start: number;
    end: number;
    code: string;
    note?: string;
}
export default function importSort(code: string, rawParser: string | IParser, rawStyle: string | IStyle): ISortResult;
export declare function sortImports(code: string, parser: IParser, style: IStyle): ISortResult;
export declare function applyChanges(code: string, changes: Array<ICodeChange>): string;
